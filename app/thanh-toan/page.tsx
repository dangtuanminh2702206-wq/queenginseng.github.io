"use client";
import Image from "next/image"; import Link from "next/link"; import { FormEvent,useState } from "react"; import { Banknote,Landmark } from "lucide-react";
import { useCart } from "@/components/cart-provider"; import { SiteHeader } from "@/components/site-header"; import { Button } from "@/components/ui/button"; import { useDemo } from "@/components/use-demo"; import { company } from "@/lib/company"; import { createOrder } from "@/lib/demo-store"; import { g8Product } from "@/lib/products"; import { assetPath,formatVnd } from "@/lib/site";
export default function CheckoutPage(){const {quantity,clear}=useCart();const {user}=useDemo();const [method,setMethod]=useState<"COD"|"BANK">("COD");const [code,setCode]=useState("");const [error,setError]=useState("");const total=quantity*g8Product.price;function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");if(!user){setError("Vui lòng đăng nhập trước khi đặt hàng.");return}const f=new FormData(e.currentTarget);try{const order=createOrder({quantity,customerName:String(f.get("name")),phone:String(f.get("phone")),province:String(f.get("province")),ward:String(f.get("ward")),address:String(f.get("address")),email:String(f.get("email")||""),note:String(f.get("note")||""),paymentMethod:method});setCode(order.code);clear()}catch(err){setError(err instanceof Error?err.message:"Không thể tạo đơn demo.")}}if(code)return <main className="min-h-screen bg-[#F7F2E7]"><SiteHeader/><section className="container-wide py-24 text-center"><p className="eyebrow">Đặt hàng thử nghiệm thành công</p><h1 className="mt-5 font-serif text-5xl">{code}</h1><p className="mx-auto mt-5 max-w-xl leading-7 text-[#587067]">Đơn hàng chỉ được lưu cục bộ trên trình duyệt, chưa gửi tới hệ thống bán hàng thật.</p><Button asChild className="mt-8 rounded-full bg-[#073D2B]"><Link href="/tai-khoan/don-hang">Xem đơn hàng</Link></Button></section></main>;return <main className="min-h-screen bg-[#F7F2E7]"><SiteHeader/><form onSubmit={submit} className="container-wide grid gap-8 py-12 lg:grid-cols-[1fr_420px]"><div className="grid gap-6"><div><p className="eyebrow">Thanh toán</p><h1 className="mt-3 font-serif text-4xl">Thông tin nhận hàng</h1></div>{!user&&<div className="rounded-xl border border-[#C8A34A] bg-white p-4 text-sm">Bạn chưa đăng nhập. <Link href="/dang-nhap" className="font-semibold underline">Đăng nhập</Link> hoặc <Link href="/dang-ky" className="font-semibold underline">đăng ký</Link> để tạo đơn demo.</div>}<section className="rounded-[2rem] bg-white p-6 sm:p-8"><div className="grid gap-4 sm:grid-cols-2"><label className="form-label">Họ và tên<input name="name" required defaultValue={user?.name||""} className="form-input"/></label><label className="form-label">Số điện thoại<input name="phone" required defaultValue={user?.phone||""} className="form-input" inputMode="tel"/></label><label className="form-label">Tỉnh/Thành phố<input name="province" required className="form-input"/></label><label className="form-label">Phường/Xã<input name="ward" required className="form-input"/></label><label className="form-label sm:col-span-2">Địa chỉ<input name="address" required className="form-input"/></label><label className="form-label">Email (không bắt buộc)<input name="email" type="email" defaultValue={user?.email||""} className="form-input"/></label><label className="form-label">Ghi chú<input name="note" className="form-input"/></label></div></section><section className="rounded-[2rem] bg-white p-6 sm:p-8"><h2 className="font-serif text-2xl">Phương thức thanh toán</h2><div className="mt-5 grid gap-3"><button type="button" onClick={()=>setMethod("COD")} className={`flex gap-3 rounded-xl border p-4 text-left ${method==="COD"?"border-[#C8A34A] bg-[#F7F2E7]":""}`}><Banknote/><span><b>COD</b><small className="block text-[#6B7C75]">Thanh toán khi nhận hàng</small></span></button><button type="button" onClick={()=>setMethod("BANK")} className={`flex gap-3 rounded-xl border p-4 text-left ${method==="BANK"?"border-[#C8A34A] bg-[#F7F2E7]":""}`}><Landmark/><span><b>Chuyển khoản ngân hàng</b><small className="block text-[#6B7C75]">Không tự xác nhận thanh toán</small></span></button></div>{method==="BANK"&&<div className="mt-6 rounded-2xl bg-[#F7F2E7] p-5"><Image src={assetPath(company.payment.qrImage)} alt="QR thanh toán MB Bank của Công ty CP Queen Ginseng Việt Nam" width={545} height={818} className="mx-auto h-auto max-w-[280px]"/><dl className="mt-4 grid gap-2 text-sm"><div><dt className="text-[#6B7C75]">Tên tài khoản</dt><dd className="font-semibold">{company.payment.accountName}</dd></div><div><dt className="text-[#6B7C75]">Số tài khoản</dt><dd className="font-semibold">{company.payment.accountNumber}</dd></div><div><dt className="text-[#6B7C75]">Số tiền</dt><dd className="font-semibold">{formatVnd(total)}</dd></div></dl><p className="mt-4 text-sm leading-6">Sau khi chuyển khoản, đơn hàng sẽ được xác nhận sau khi Queen Ginseng kiểm tra giao dịch.</p></div>}</section></div><aside className="h-fit rounded-[2rem] bg-[#052C20] p-7 text-white lg:sticky lg:top-28"><h2 className="font-serif text-2xl">Đơn hàng</h2><p className="mt-6 text-white/70">{g8Product.name} × {quantity}</p><p className="mt-2 text-sm text-white/50">30 gói × 15 g · 450 g</p><div className="mt-6 flex justify-between border-t border-white/15 pt-5"><span>Phí giao hàng</span><span>[...]</span></div><div className="mt-3 flex justify-between text-lg font-semibold"><span>Tổng cộng</span><span>{formatVnd(total)}</span></div>{error&&<p role="alert" className="mt-5 rounded-xl bg-red-950/40 p-3 text-sm">{error}</p>}<Button disabled={!quantity} className="mt-7 h-12 w-full rounded-full bg-[#C8A34A] text-[#052C20]">Tạo đơn hàng thử nghiệm</Button><p className="mt-4 text-xs leading-5 text-white/55">Đây là bản demo. Không có giao dịch hoặc xác nhận đơn hàng thật.</p></aside></form></main>}
